[package]
name = "aurum-editor"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Aurum editor bridge: a stable GDExtension surface so tools can drive the live Godot editor."

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = { workspace = true }
serde_json = { workspace = true }
